clear all;
close all;
clc;
k=-20:20;
x1=xk(k);
subplot(4,1,1);
stem(k,x1);
grid on;
title('x[k]');
x2=xk(-1*k);
subplot(4,1,2);
stem(k,x2);
grid on;
title('x[-k]');
x3=xk(2*k+2);
subplot(4,1,3);
stem(k,x3);
grid on;
title('x[-k]');
x4=xk(k/2);
subplot(4,1,4);
stem(k,x4);
grid on;
title('x[k/2]');
